export enum SupportSessionStatus {
  OPEN = 'OPEN',
  ACTIVE = 'ACTIVE',
  CLOSED = 'CLOSED',
}

export enum SupportMessageSenderRole {
  CUSTOMER = 'CUSTOMER',
  STAFF = 'STAFF',
}

export enum SupportSessionAssignedFilter {
  Me = 'me',
  Unassigned = 'unassigned',
  Any = 'any',
}
