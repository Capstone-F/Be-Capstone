export enum EscrowHoldSourceType {
  CONSULTATION = 'CONSULTATION',
  TREATMENT_PHASE = 'TREATMENT_PHASE',
}

export enum EscrowHoldStatus {
  HELD = 'HELD',
  RELEASED = 'RELEASED',
  REFUNDED = 'REFUNDED',
}

export enum ClinicWithdrawalStatus {
  REQUESTED = 'REQUESTED',
  PAID = 'PAID',
  REJECTED = 'REJECTED',
}
