export enum TimeOfUse {
  AM = 'AM',
  PM = 'PM',
  AM_PM = 'AM_PM',
}

export enum LabelMatchType {
  REQUIRED = 'REQUIRED',
  OPTIONAL = 'OPTIONAL',
  EXCLUDED = 'EXCLUDED',
}

export enum SkinTypeRecommendation {
  RECOMMENDED = 'RECOMMENDED',
  AVOID = 'AVOID',
}
