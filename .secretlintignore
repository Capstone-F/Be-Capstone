node_modules
dist
build
coverage
.eslintcache
package-lock.json
*.min.js
*.lock

# Local env copies (`cp .env.*.example`); not scanned so `npm run secretlint` passes on dev machines.
# Do not commit real secrets in tracked files — CI has no `.env.dev` anyway.
.env.dev
.env.prod
